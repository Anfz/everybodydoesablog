import posts_json from './data/posts.json';

import { ADD_BLOG_POST } from '../actions';

function blogPosts(state = characters_json, action){
  //got nothing to do anyway :D 
  return state; 
}

const rootReducer = blogPosts; 

export default rootReduer; 